/**
 * The "How to use" guide link, shown in the nav and on the login page.
 * Defaults to the in-app illustrated guide (/guide); override with
 * NEXT_PUBLIC_GUIDE_URL to point at an external doc instead.
 */
export const GUIDE_URL = process.env.NEXT_PUBLIC_GUIDE_URL || "/guide";
